name: 🔧 Self-Healing

on:
  schedule:
    - cron: '*/30 * * * *'  # Every 30 minutes
  workflow_dispatch:
  workflow_run:
    workflows: ["🚀 Auto Deploy"]
    types: [completed]

jobs:
  monitor:
    name: Monitor Deployments
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Check Health
        id: health
        run: |
          if [ ! -z "${{ secrets.DEPLOY_URL }}" ]; then
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${{ secrets.DEPLOY_URL }}/api/health" --max-time 10 || echo "000")
            echo "status=$STATUS" >> $GITHUB_OUTPUT
            echo "Health check status: $STATUS"
          else
            echo "status=skip" >> $GITHUB_OUTPUT
            echo "No DEPLOY_URL configured, skipping health check"
          fi

      - name: Auto-Rollback
        if: steps.health.outputs.status != '200' && steps.health.outputs.status != 'skip' && steps.health.outputs.status != '000'
        run: |
          echo "🚨 Health check failed (Status: ${{ steps.health.outputs.status }})"
          echo "Triggering rollback to previous commit..."
          PREV_SHA=$(git rev-parse HEAD~1)
          echo "Rolling back to: $PREV_SHA"
          gh workflow run "🚀 Auto Deploy" --ref "$PREV_SHA" || echo "⚠️ Could not trigger rollback workflow"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Attempt Auto-Fix
        if: steps.health.outputs.status != '200' && steps.health.outputs.status != 'skip' && steps.health.outputs.status != '000'
        run: |
          echo "🔧 Attempting automatic fixes..."
          if [ -f "package.json" ]; then
            npm ci || true
            npm run build || true
          fi

      - name: Create Issue on Failure
        if: failure()
        uses: actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea
        with:
          script: |
            const existing = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: 'deployment',
              state: 'open',
            });
            const hasExisting = existing.data.some(i => i.title.includes('Health Check Failed'));
            if (hasExisting) {
              console.log('Open health check issue already exists, skipping.');
              return;
            }
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: '🚨 Self-Healing: Deployment Health Check Failed',
              body: `Deployment health check failed.\n\nStatus: ${{ steps.health.outputs.status }}\nWorkflow: ${context.workflow}\nRun: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              labels: ['bug', 'deployment', 'auto-generated']
            })

  dependency-updates:
    name: Auto Update Dependencies
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node
        if: hashFiles('package.json') != ''
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
        with:
          node-version: '20'

      - name: Update npm dependencies
        if: hashFiles('package.json') != ''
        run: |
          npm update
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name "BlackRoad Bot"
            git config user.email "bot@blackroad.io"
            git add package*.json
            git commit -m "chore: auto-update dependencies"
            git push
          fi
